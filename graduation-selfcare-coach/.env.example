OPENAI_API_KEY=sk-...

# ── 관측성 (강의 #14.5) — 선택 ────────────────────────────────────────
# 켜면 노드 하나하나의 입출력·토큰·비용·지연이 LangSmith에 남는다.
# 이 코치는 한 세션에 LLM이 19번 도니, 어디서 시간과 돈이 새는지 눈으로 봐야 안다.
#
# ⚠️ 켜면 **대화 내용이 LangSmith 서버로 전송된다.** 개인 건강 기록이 오가는 앱이라
#    기본은 꺼둔다. 개발 중에만 켜고, 배포본에는 넣지 말 것.
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=lsv2_...
# LANGSMITH_PROJECT=selfcare-coach
